
@model IEnumerable<CoverType>
@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Cover Type List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="CoverType" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> &nbsp; New Cover Type
            </a>
        </div>
    </div>
</div>
<br />

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>CoverType Name</th>
            <th>Edit</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var obj in Model)
        {
            <tr>
                <td width="50%">@obj.Name</td>              
                <td width="30%">
                    <div class="btn-group" role="group">
                        <a asp-controller="CoverType" asp-action="Edit" asp-route-id=@obj.Id class="btn btn-group btn btn-primary mx-3" role="group">
                            <i class="bi bi-pencil-square">Edit</i>
                        </a>
                        <a asp-controller="CoverType" asp-action="Delete" asp-route-id=@obj.Id class="btn btn-group btn btn btn-danger mx-3" role="group">
                            <i class="bi bi-trash">Delete</i>
                        </a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>




